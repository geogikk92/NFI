// Паролата на сийднатите профили за РАЗРАБОТКА — единственият източник.
//
// ЧИСТ модул, без база и без "server-only": внася се и от сийда, и от
// скриптовете, и от тестовете.
//
// ─────────────────────────────────────────────────────────────────────────
//  ЗАЩО СЪЩЕСТВУВА ТОЗИ ФАЙЛ
// ─────────────────────────────────────────────────────────────────────────
// До 17.08.2026 стойността беше преписана на ЧЕТИРИ места и се беше
// разминала на две различни пароли:
//
//   prisma/seed.ts                        „1"
//   lib/auth/login-db.integration.test.ts „1"
//   e2e/_harness.mjs                      „nfi-lokalna-parola"
//   scripts/e2e-cleanup.ts                „nfi-lokalna-parola"
//
// Следствието беше, че двата пакета проверки се ИЗКЛЮЧВАХА взаимно:
// каквото и да имаше в базата, единият пада. И то не с ясно съобщение, а
// с „вярна парола не праща към /admin" — тоест изглеждаше като счупен
// вход, а не като разминала се константа. Отне час, докато се намери.
//
// Стойността е тази от сийда, защото сийдът СЪЗДАВА състоянието на базата.
//
// ─────────────────────────────────────────────────────────────────────────
// Стои открито в кода нарочно и това е безопасно по три причини:
// сийдът отказва да пише в нелокална база, НЕ пипа вече съществуваща
// парола, а в живата база паролата се задава с отделна команда, която не
// пази нищо:
//
//     DATABASE_URL="…" npm run admin:password -- admin@nfi.local

export const DEV_PASSWORD = "1";

/** Профилите, които сийдът създава. Ползват се и при чистенето. */
export const SEEDED_ACCOUNTS = [
  "admin@nfi.local",
  "student@nfi.local",
] as const;
