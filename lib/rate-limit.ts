// Имената на действията, които се броят за ограничението по IP.
//
// ЧИСТ модул, нарочно отделен от lib/rate-limit-db.ts. Причината е същата
// като при lib/admin/limits.ts: онзи файл започва с `import "server-only"`
// и води до Prisma, тоест не може да се внесе от обикновен скрипт. А
// точно скрипт има нужда от тези имена — scripts/e2e-cleanup.ts трие
// броячите между пусканията.
//
// ЗАЩО ГИ ТРИЕ: e2e/vhod.mjs нарочно опитва вход с грешна парола. Всеки
// такъв опит е ред тук и входът се заключва при 10 неуспеха в прозореца.
// Без чистене след няколко пробега вярната парола спира да работи и
// осем от единайсетте проверки падат — без нито един сгрешен ред код.
// (Случи се на 05.08.2026 с натрупани 375 реда.)
//
// Изброени на ЕДНО място, за да не се разминат между мястото, което пише,
// и мястото, което брои — разминаят ли се, ограничението тихо спира да
// работи, а никой тест няма да го хване.

export const RATE_ACTIONS = {
  loginFailed: "auth.login.failed",
  register: "auth.register",
  levelTest: "content.level-test.submit",
} as const;

export type RateAction = (typeof RATE_ACTIONS)[keyof typeof RATE_ACTIONS];
