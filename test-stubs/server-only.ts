// Празен заместител на пакета „server-only" за тестовете.
//
// Истинският пакет хвърля при внасяне навсякъде извън серверен компонент —
// така пази тайни от клиентския бъндъл. Под vitest в node среда обаче няма
// клиентски бъндъл, тоест пазачът само блокира тестването на серверни
// модули (lib/db-health.ts, lib/admin/queries.ts, lib/consent-cookie.ts).
//
// Заместването е в vitest.config.ts, НЕ в tsconfig — приложението и билдът
// продължават да виждат истинския пакет и защитата остава непокътната.
export {};
